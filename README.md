# CHAT

Chat sencillo, utilizando React, Webosocker, NodeJS y Material-UI

## Instalación

Clonar el repositorio

`$ git clone url`

Luego de clonar, en la raíz del proyecto, tomar la rama feat/commit, ejecutar el comando `docker-compose up --build -d`

Esperar que finalice de hacer la instalación de dependencias.

## Probando

Acceder al navegador `http://localhost:3000/`  y pulsar el botón en la parte inferior izquierda y completar el formulario para iniciar con una conversación.

Para el Agente se debe acceder a `http://localhost:3000/agent` e ingresar `1020304050` para poder "loguearse".